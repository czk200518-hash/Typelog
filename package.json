{
  "name": "obsidian-typelog",
  "version": "1.0.95",
  "description": "Typing stats: net vs gross words, active time, speed, goals, heatmap, trends and reports.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "install:vault": "powershell -ExecutionPolicy Bypass -File scripts/install.bat"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "typing",
    "statistics",
    "typelog"
  ],
  "author": "TypeLog",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.21.0",
    "happy-dom": "^20.11.1",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
